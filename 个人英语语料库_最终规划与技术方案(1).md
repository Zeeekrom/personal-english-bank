# Personal English Bank 最终规划与技术方案

> 版本：Final Planning v1.3（Curated Corpus Workflow）
> 项目名称：Personal English Bank / Personal English OS
> 项目目标：把真实录音、个人表达和澳洲英语输入，持续转化为可以检索、复习、练习并在真实生活中主动使用的个人英语语料；同时将系统建设为一个对标塔斯马尼亚企业技术栈的云应用开发项目。

---

# 修订记录

## 2026-07-26｜v1.3｜精修语料工作流重构

本次修订重新定义第一阶段的数据边界：系统不再把未经处理的原始转写直接导入数据库，也不再要求用户在系统中逐段筛选。原始材料先由 Codex 在系统外完成转写、翻译、保真整理和精修筛选；只有最终确认的双语句子通过稳定 API 原子写入数据库。

第一阶段的唯一主流程为：

```text
本地输入
├── MP3 / MP4 → Codex 调用本地转写工具生成文字
└── 用户已准备的文字 → 直接交给 Codex
        ↓
保真双语版：一句英文 + 一句中文，保留不确定、误录和翻译风险
        ↓
精修双语版：纠正明显误录、补全语义、删除无用内容并筛选学习句
        ↓
Codex 生成 Curated Import Package（结构化 JSON）
        ↓
API 原子写入 Source / Asset / Transcript / Segment / Learning Item / Review Schedule
        ↓
按文件、日期、摘要、英文、中文和来源查询
        ↓
每日按 1 / 3 / 7 / 14 / 30 天间隔复习
        ↓
用户口述一次并点击完成；系统不评价口述内容
```

确定的产品规则：

1. 音频/视频和现成文本只是两种输入方式，进入转写文字后共享同一条处理流水线；
2. 每个来源同时保留保真双语版和精修双语版，不能用精修结果覆盖原始证据；
3. 无法判断是录音不清、转写错误还是原始表达错误时，在保真版标注不确定；精修版根据上下文恢复最完整、自然且不虚构事实的表达；
4. 筛选、翻译、纠错、意图、主要问题、改进版本和摘要在第一阶段由 Codex 完成，不要求用户在 Web 页面逐句标注；
5. 只有 `keep` 的精修双语句子进入学习库；废话、纯重复、无法恢复语义和没有学习价值的内容保留在外部处理档案但不进入复习数据库；
6. 数据库以句子为最小学习单位，以导入文件为上层来源单位；每句话必须能追溯到文件名、发生日期、句序和可用时间戳；
7. 每个来源必须有简短中文摘要，帮助用户快速判断发生日期、场景和主要内容；
8. Source 与 Learning Item 必须提供 Create、Read、Update、Delete；删除来源时同时清理其派生语料、复习计划和历史；
9. 第一阶段应用运行时不调用 AI、云服务或服务器。Codex 是临时的外部 Curator，通过公开、版本化、可校验的导入 API 代替未来 AI Workflow；
10. 未来 AI Provider 必须复用同一 Curated Import Contract，不得绕过校验直接写数据库。

## 2026-07-26｜v1.2｜MVP 执行基线

本次修订不推翻长期产品与 Azure 技术方向，而是将“学习产品验证”和“企业云作品集扩展”分成两条有先后关系的路线。

确认的主要问题：

1. 原 Phase 1 同时包含 Azure SQL、Blob、Container Apps、Key Vault、Managed Identity、监控、Terraform、全文搜索、前后端和数据库迁移，范围超过个人产品 MVP；
2. 每日复习原本位于 Phase 3，但它属于最早需要验证的核心学习闭环；
3. 第一条端到端 User Story 同时依赖 Phase 2 的 AI 和 Phase 3 的复习功能，与阶段定义不完全一致；
4. 产品目标与求职作品集目标都合理，但不能以相同优先级同时展开；
5. 数据模型需要进一步区分逻辑来源与物理文件、全局人物与单次转写说话人、学习生命周期与使用模式；
6. 需要补充真实使用记录、异步任务幂等、数据所有权、删除与保留政策；
7. Prisma 的 SQL Server Connector 不直接支持 Prisma `Json` 类型。JSON 数据在 SQL Server 中应使用 `NVARCHAR(MAX)`、`ISJSON` 约束和受控 SQL Migration，关键可查询字段应正常化；
8. 需要在进入大规模云端处理前定义月度预算、每日新增上限、AI 接受率、事实保持率和真实使用率等验收指标。

本次确认的执行原则：

- **Product Core 优先**：先证明“Codex 精修真实材料 → 精修双语句子入库 → 到期复习 → 记录完成”对英语学习有用；
- **Portfolio Expansion 后置**：Azure、Terraform、Service Bus、可观测性和多 Provider 在核心闭环可用后逐步加入；
- 第一阶段应用内部不接 AI、不接云端语音服务、不做 Notion 双向同步；音视频由 Codex 在系统外调用本地工具转写；
- 第一阶段使用本地 SQL Server Developer Edition，保持与未来 Azure SQL 相同的 T-SQL 和 Migration；
- 第一阶段从配置的 `TRANSLATION_ROOT` 中分批处理已经完成精修的来源，不设固定 5 个文件上限，不移动、不覆盖原文件；未完成来源只登记清单，等待后续 Curator 处理；
- 未精修的原始文本不写入学习数据库；Codex 同时生成保真双语版、精修双语版和 Curated Import Package；
- 每个通过筛选的精修句子在一次数据库事务中创建 Segment、Learning Item 和 Review Schedule；
- 每天必须有可控的复习上限，避免导入速度超过实际学习能力。

## MVP v0.1 的完成定义

当用户能够完成以下流程时，第一阶段成立：

```text
从 Translation 目录取得一个 MP3 / MP4 或已转写文本
→ Codex 转写（如需要）并生成逐句保真双语版
→ Codex 生成逐句精修双语版、文件摘要并筛选学习句
→ Codex 通过 Curated Import API 一次性创建 Source、Asset、Transcript、Segment 和 Learning Item
→ 条目进入 1/3/7/14/30 天复习计划
→ 用户口述一次并点击完成，系统不判断口述内容是否正确
→ 用户可按文件、日期、摘要、英文和中文查询，并可增删改查
```

MVP v0.1 明确不包含：

- Azure OpenAI 或 Anthropic；
- Azure AI Speech；
- 应用运行时的自动转写、翻译、语法、发音或自然度判断；
- Service Bus 和独立 Worker；
- Notion 同步；
- 语义搜索；
- 多用户与公开注册；
- 应用运行时自动生成学习条目（第一阶段由 Codex 在系统外生成结构化导入包）。

---

# 0. 本文档的定位

本文档合并了以下三类内容：

1. 现有 Notion「Personal English Bank」中的项目定位、Layout、Prepared Expression、Interaction Log、自我介绍与相关草稿；
2. 本地 Markdown《个人英语语料库构建方案》中的结构、字段、模板和维护方法；
3. 后续讨论确定的新要求：
   - 分析自己在真实录音中的习惯性用语、语法、用词、意思偏差和流畅度；
   - 从澳洲老师、本地人、课堂和会议录音中提取自然表达、句式、词汇和语用；
   - 将面试、Networking 与职业资料按项目边界选择性导入；
   - 建立「录音 → 转写 → AI 分析 → 人工审核 → 数据库 → 每日复习」的自动化 Web 平台；
   - 支持已读/未读、顺序处理、自定义导入、每日复习和复习历史；
   - 支持中文个人录音转为自然、简单、可主动使用的英文个人表达；
   - 将该系统作为一个可由 Codex 持续开发和维护的软件项目。

本文档既是内容规划，也是后续 Codex 开发项目的产品需求与技术蓝图。

---

# 1. 项目核心定位

Personal English Bank 不是普通单词本，也不是把所有录音转写、PDF 和课堂内容搬进 Notion。

它是一套属于 Evan 的个人英语操作系统，主要解决四个问题：

1. **临场表达困难**：知道中文意思，但无法快速、自然地组织英文；
2. **真实交流复盘不足**：当时没听懂、说错、说得不自然，但之后没有形成可复用表达；
3. **输入没有转化为输出**：听过大量课程和本地人表达，却没有变成自己可以主动使用的英语；
4. **材料不断增加但无法复习**：录音和文本越来越多，缺少处理顺序、状态、复习计划和真实使用记录。

最终要形成下面的循环：

```text
真实生活、课堂、项目和个人思考
            ↓
录音与原始材料
            ↓
语音转写和文本导入
            ↓
说话人、场景和内容分类
            ↓
发现表达缺口与高价值表达
            ↓
AI 生成结构化建议
            ↓
人工审核、修改和确认
            ↓
进入主动语料库与复习队列
            ↓
口语练习和真实使用
            ↓
再次修改、升级或标记为已掌握
```

项目真正衡量的不是收集了多少内容，而是：

- 能否更快开口；
- 能否更清楚地介绍自己；
- 能否听懂澳洲课堂和日常交流中的高频表达；
- 能否接住对方的话并继续追问；
- 能否减少反复出现的语法、用词和表达问题；
- 能否把个人经历、想法和性格形成稳定的英文表达；
- 能否让真实交流一次比一次更自然。

---

# 2. 项目边界

## 2.1 Personal English Bank 负责什么

本项目只保留与「英语理解、英语表达和真实练习」直接有关的内容：

- 日常生活和学校交流；
- 课堂、Tutorial、小组任务和学校服务；
- 社团、志愿活动和 Networking 对话；
- 项目介绍和专业交流；
- 自我介绍、个人故事、兴趣、观点、价值观和个人特质；
- 自己说过的话及其改进版本；
- 澳洲老师和本地人的自然表达；
- 高频词汇、固定搭配、句型和发音；
- 对话补救、提问、追问、转场和结束表达；
- 面试与求职材料中可以直接变成口语表达的部分；
- 中文个人录音转为简单、自然、适合自己主动使用的英文。

## 2.2 MyLinkedIn 项目负责什么

完整的职业与求职方法论放入 MyLinkedIn，而不是复制到本项目：

- 职业定位和目标岗位选择；
- Competitive Edge 的战略判断；
- 公司、岗位和 Job Description 分析；
- 简历、Cover Letter、LinkedIn 和 Selection Criteria；
- STAR 案例与具体岗位的匹配；
- 面试流程、公司研究、薪资和 Offer 决策；
- 针对具体岗位的面试策略。

Personal English Bank 只接收 MyLinkedIn 已经确定的最终语言材料，例如：

- 10/30/60 秒职业定位；
- 2–3 项核心优势的英文表达；
- 已选定 STAR 故事的口语版本；
- 面试中的澄清、思考和补救表达；
- 面试后对自己实际回答的复盘。

## 2.3 不进入主动语料库的内容

以下内容可以保存在原始档案，但默认不进入主动复习：

- 与自己近期生活和表达无关的整篇教材；
- 课堂录音中所有技术细节的逐句摘录；
- 只为显得高级而收集的复杂表达；
- 不理解或不会真实使用的俚语；
- 自动转写中无法确认的错误句子；
- 纯粹重复、低质量或无上下文的片段；
- 完整求职理论和通用面试清单。

---

# 3. 四类核心语料来源

## 3.1 My Spoken Output｜自己的真实英语输出

来源包括：

- 与老师、同学、工作人员和陌生人的对话；
- 社团、志愿活动和 Networking；
- 项目会议和小组任务；
- 银行、住宿、购物、交通、医疗和电话；
- 面试、咨询和职业活动；
- 英文自言自语和口语练习。

处理目标：

- 识别习惯性用语和过度重复的填充词；
- 检查语法、用词、搭配和句式；
- 判断是否「想表达 A，却实际说成 B」；
- 区分真正错误、发音问题和转写错误；
- 提供最小修正版、自然版和 Evan 易说版；
- 检查回答是否过长、偏题、没有重点或缺少追问；
- 将反复出现的问题形成个人错误模式。

每条问题必须区分：

- `Confirmed Error`：能够确认的语言错误；
- `Likely Pronunciation Issue`：可能因发音导致；
- `Possible Transcription Error`：更可能是转写软件错误；
- `Needs Audio Check`：必须回听原音频才能判断；
- `Meaning Mismatch`：实际表达与原本意图不一致；
- `Naturalness`：语法基本正确但不自然；
- `Fluency / Structure`：表达组织、重复或对话推进问题。

## 3.2 Australian Native Input｜澳洲老师与本地人输入

来源包括：

- 本地老师现场课堂；
- 录播视频；
- Tutorial 与小组交流；
- 学校工作人员；
- 社团、项目和活动中的本地人；
- 会议、演讲和日常对话。

重点提取：

- 高频口语块，而不是孤立单词；
- 开始解释、转换话题、举例、总结和确认理解的句式；
- 礼貌、委婉拒绝、邀请提问和结束交流的语用；
- 澳洲常见词汇、缩写、语气和发音；
- 自己未来可能主动使用的表达；
- 只需要听懂但暂时不适合模仿的表达。

每条本地表达必须标记：

- `Active Use`：建议主动使用；
- `Understand Only`：先保证听懂；
- `Context-specific`：只适合特定场景；
- `Speaker Habit`：可能只是个人习惯；
- `Too Informal`：当前不建议主动模仿。

不要把本地人的所有口语都当成标准答案。自然口语同样包含重复、停顿、残句、口误和个人化表达。

## 3.3 Prepared Self-Expression｜主动准备的个人表达

来源包括：

- 个人身份、教育和经历；
- 实习、创业、项目和职业方向；
- AI Factory、Motorsport LiDAR、AR/VR、游戏和软件项目；
- 兴趣、书籍、电影、动画、AI、游戏和社会议题；
- 个人价值观、性格、优势、不足和人生规划；
- 中文自言自语录音；
- 即将发生的重要会议、活动和 Networking。

同一内容可以准备：

- 10 秒版本；
- 30 秒版本；
- 60 秒版本；
- 2 分钟详细版本；
- Casual / Neutral / Formal 版本；
- 面向同学、老师、项目伙伴或雇主的不同版本。

## 3.4 Vocabulary, Patterns and Pronunciation｜词汇、搭配与发音

词汇分为：

1. `Personal Active Vocabulary`：近期真实会使用；
2. `Receptive Vocabulary`：目前主要用于听懂；
3. `Technical Vocabulary`：课程、项目和职业相关；
4. `Collocation / Pattern`：固定搭配和句型；
5. `Pronunciation Focus`：人名、地名、专业词和反复识别错误的词。

词汇不能只保存中文释义，应包含：

- 简单英文解释；
- 常见搭配；
- 自己的例句；
- 来源和上下文；
- 发音与重音；
- 常见错误；
- Active / Understand Only；
- 下次复习时间。

---

# 4. 不同原始文件的处理策略

## 4.1 含大量 Evan 说话的真实对话

建议权重：

```text
60% 分析自己的表达
20% 提取对方的自然表达
10% 词汇与发音
10% 对话结构与复盘
```

每个文件优先产出：

- 1 条 Interaction Log；
- 3–8 个主要表达问题；
- 3–8 个 Better Versions；
- 2–5 个值得学习的对方表达；
- 1–5 个词汇、搭配或发音项；
- 必要时生成一个可复用的个人话题卡片。

## 4.2 老师现场课堂录音

每小时录音最多优先提取：

- 5–10 个自然口语块；
- 5–10 个专业搭配；
- 3–5 个课堂互动或求助句；
- 需要重点掌握的人名、课程名和术语发音。

不逐句分析老师整节课，也不把全部专业知识复制到语料库。

## 4.3 录播视频

重点提取：

- 有组织的解释句式；
- 专业内容的表达方式；
- 结构化陈述、举例和总结语言；
- 正式程度略高、但仍可复用的表达。

## 4.4 纯演讲或自己未参与的会议

只有在以下情况下进入主动语料库：

- 与自己的兴趣、专业或近期交流直接相关；
- 出现可复用的自然表达；
- 未来需要讨论这个话题；
- 具有澳洲语用或口语学习价值。

否则只归档和索引，不进入每日复习。

## 4.5 中文个人录音

处理流程：

```text
中文录音
  ↓
保留中文原始转写
  ↓
提取事实、经历、观点、性格和想表达的核心意思
  ↓
生成 Easy Active English
  ↓
生成 Natural English 和不同长度版本
  ↓
拆分为个人叙事、故事、观点或词汇条目
  ↓
人工确认
  ↓
导入主动语料库
```

必须保留中文原文，不能只留下英文翻译。中文原文是判断英文是否准确表达原意的依据。

---

# 5. 端到端产品工作流

## 5.0 第一阶段当前生效工作流

第一阶段以本节为准；5.1–5.8 中涉及系统内上传、云端转写、运行时 AI 建议和用户逐条审核的内容属于后续目标，不是当前实现。

```text
输入 A：本地 MP3 / MP4
→ Codex 调用本地转写工具
→ 转写文字

输入 B：用户已转好的文字
→ 直接进入文字处理

两种输入汇合
→ Codex 逐句翻译为“英文一行、中文一行”
→ 保存保真双语版，不掩盖听不清、误录和翻译不确定性
→ 生成精修双语版，纠错、补全、去除废话和无学习价值内容
→ Codex 完成摘要、主要问题、中文意图、改进表达和最终筛选
→ 生成 `*.curated.json`
→ API 校验并一次性写入数据库
→ 用户按来源文件、日期、摘要和双语句子查询
→ 每天按固定间隔复习；口述一次并点击完成，不做正确性审查
```

第一阶段没有“用户像打标签一样逐句 Accept/Reject”的环节。Codex 先完成筛选，Web 中只呈现已经可学习的结果；用户仍可通过 CRUD 接口和页面修改或删除来源与句子。

## 5.1 输入阶段

系统支持：

- 上传音频；
- 上传 TXT、MD、DOCX 或字幕文件；
- 粘贴文本；
- 批量导入文件夹；
- 手动新建个人表达；
- 连接外部转写服务的导出文件；
- 后期可增加手机录音入口。

每个来源首先进入 `Inbox`，不直接进入主动语料库。

## 5.2 转写阶段

转写可以来自两类方式：

1. 现有在线服务导出：Notta、Otter、Zoom 等；
2. 系统云端转写：Azure AI Speech Provider。

系统通过 `TranscriptionProvider` Adapter 接收统一结果，因此未来可以更换云端供应商，而不改变领域模型和数据库结构。本地开发只使用 Mock Provider 或固定测试文件，不部署本地转写模型。

转写结果应尽量保留：

- 开始和结束时间；
- 说话人；
- 原始语言；
- 段落或句子；
- 转写置信度（如服务提供）；
- 原音频位置。

## 5.3 预处理阶段

系统自动执行：

- 文件去重；
- 文本编码和格式清理；
- 按时间或段落切分；
- 初步识别语言；
- 初步说话人标记；
- 场景和内容类型分类；
- 标记是否包含 Evan 的讲话；
- 生成处理优先级；
- 检查是否与已有文件重复。

说话人无法自动确定时，进入人工标记界面。

## 5.4 AI 分析阶段

根据来源类型选择不同 Prompt：

- `analyse_my_spoken_output`；
- `extract_native_expressions`；
- `extract_vocabulary_and_patterns`；
- `convert_chinese_narrative_to_english`；
- `prepare_interaction_log`；
- `deduplicate_learning_items`；
- `generate_review_question`。

AI 必须输出符合 JSON Schema 的结构化结果，而不是一整段无法自动读取的散文。

示例结构：

```json
{
  "source_id": "uuid",
  "interaction_summary": "string",
  "suggestions": [
    {
      "type": "my_better_version",
      "original_text": "string",
      "chinese_intention": "string",
      "problem_types": ["word_choice", "meaning_mismatch"],
      "minimum_correction": "string",
      "natural_version": "string",
      "easy_active_version": "string",
      "explanation_cn": "string",
      "confidence": 0.86,
      "requires_audio_check": true,
      "usage_mode": "active",
      "tags": ["daily_life", "volunteering"]
    }
  ]
}
```

## 5.5 验证与人工审核阶段

AI 不直接写入最终语料表。

正确流程是：

```text
AI JSON 输出
   ↓
Schema 验证
   ↓
写入 AI Suggestions / Staging
   ↓
用户审核：接受、修改、拒绝、合并
   ↓
事务性写入正式表
```

每条建议都应显示：

- 原始上下文；
- 音频时间点；
- AI 建议；
- 置信度；
- 是否需要回听；
- 可能属于转写错误还是语言错误；
- 接受、编辑、合并、忽略按钮。

## 5.6 进入学习与复习阶段

确认后的条目进入：

- `Ready`：已确认，等待第一次练习；
- `Learning`：正在学习；
- `Active`：已经可以主动使用；
- `Mastered`：稳定掌握；
- `Understand Only`：主要用于听懂；
- `Archived`：不再复习但保留记录。

系统每天生成一个 Review Queue：

1. 到期复习；
2. 尚未读过的新条目；
3. 近期真实场景最相关的条目；
4. 反复出错的条目；
5. 用户手动置顶的内容。

---

# 6. “好或坏”应如何判断

系统不能简单给一句话打“正确/错误”。应按维度分析。

## 6.1 自己的英语输出

每个片段按以下维度评估：

- `Grammar`：语法；
- `Word Choice`：用词；
- `Collocation`：搭配；
- `Meaning Alignment`：是否准确表达原意；
- `Naturalness`：是否自然；
- `Clarity`：是否清楚；
- `Conciseness`：是否过长、重复；
- `Fluency`：填充词、停顿和重启；
- `Interaction`：是否回应问题、继续追问或给对方说话空间；
- `Register`：正式程度是否适合场景。

输出三个英文版本：

1. `Minimum Correction`：只修明显错误；
2. `Natural Version`：更自然的英语；
3. `Easy Active Version`：最适合 Evan 记忆和主动使用的简单版本。

## 6.2 澳洲老师和本地人的输入

按以下维度判断是否值得学习：

- 是否高频；
- 是否可跨场景复用；
- 是否符合 Evan 当前表达水平；
- 是否自然但不过度口语化；
- 是否只是说话人个人习惯；
- 是否适合主动使用；
- 是否有听力价值；
- 是否能作为句型或搭配整体记忆。

## 6.3 发音

仅凭转写文本不能可靠判断发音。

文本只能产生线索：

- 同一词反复被识别成别的词；
- 对方多次要求重复；
- 专业词、人名和地名持续识别错误；
- 语法正常但整句无法识别。

真正的发音分析必须引用原音频片段，并标记：

- `Suspected from transcript`；
- `Confirmed from audio`；
- `Needs practice`。

第一版系统可以只支持“发音待检查”和播放对应音频，不必一开始就做自动音素评分。

---

# 7. 数据库总体设计

## 7.1 最终数据库选择：Microsoft SQL Server / Azure SQL Database

本项目最终选择：

- **数据库引擎与 SQL 方言：Microsoft SQL Server / T-SQL**；
- **云端生产数据库：Azure SQL Database**；
- **本地开发数据库：SQL Server Developer Edition 容器**；
- **ORM 与迁移：Prisma ORM + Prisma Migrate / 受控 SQL Migration**。

这里的目标不是选择最方便的个人项目数据库，而是选择对塔斯马尼亚就业和企业技术栈更有迁移价值的方案。

### 选择依据

截至 2026-07-26，对塔斯马尼亚公开采购信息、企业招聘和技术岗位样本的研究显示：

- 塔斯马尼亚州政府存在覆盖政府机构的 Microsoft Agreement 和 Microsoft Licensing Solutions Provider 合同；
- TasPorts 的相关岗位明确涉及 Microsoft 365 与 Azure；
- Hydro Tasmania 的软件岗位出现 .NET/C#、Azure DevOps Server 与 GitHub；
- Tassal 的 Hobart 企业应用岗位同时出现 Power Platform、Dynamics 365、Azure Functions、Logic Apps、Azure DevOps，以及 Dataverse/SQL 类关系数据库；
- Hobart 的数据与 BI 岗位反复出现 SQL Server、SSIS、SSAS、Power BI 和 Microsoft 数据生态。

这不能被当成塔州所有企业技术栈的完整统计，但它提供了比“全球流行度”更直接的本地职业信号。

### 为什么不把其他数据库作为主库

| 数据库                 | 本项目决定       | 原因                                                                                                   |
| ---------------------- | ---------------- | ------------------------------------------------------------------------------------------------------ |
| SQLite                 | 不作为生产主库   | 适合单机原型和测试，但无法充分展示企业级并发、权限、部署、运维和云数据库能力。                         |
| MySQL                  | 不作为主库       | 全球应用广泛，但本次塔州公开岗位样本中，本地 Microsoft 数据栈信号更强。                                |
| PostgreSQL             | 不作为本项目主库 | 技术能力很强，也适合现代云应用；但为了对齐塔州 Microsoft/Azure/SQL Server 生态，本项目改用 Azure SQL。 |
| Oracle                 | 不作为主库       | 在大型机构和遗留系统中仍有价值，但成本、复杂度和本项目职业回报不匹配。                                 |
| SQL Server / Azure SQL | **最终选择**     | 与 Azure、.NET、Power BI、Power Platform、政府和本地企业应用生态形成完整技术链。                       |

### 仍然需要掌握的可迁移能力

选择 Azure SQL 不代表只学习某个厂商按钮。数据库设计仍必须覆盖：

- 关系模型、主键、外键和约束；
- Normalisation；
- Transaction 与并发；
- Index 与执行计划；
- View、Stored Procedure 与受控 T-SQL；
- JSON 数据验证与查询；
- Full-Text Search；
- Migration、备份、恢复和审计；
- 权限、最小权限原则和 Secret Management。

## 7.2 数据库事实来源与环境划分

项目成熟后的唯一主数据源为 **Azure SQL Database**。

### 本地开发环境

本地开发不等于本地生产，也不等于使用本地 AI：

- 使用 Docker/容器运行 SQL Server Developer Edition；
- 使用与生产一致的 T-SQL、Schema 和 Migration；
- 本地文件可用于开发测试，但正式文件写入云端对象存储；
- AI 调用使用 Mock Provider 或云端测试环境，不部署 Ollama 等本地模型；
- 通过 `.env.local` 或开发 Secret Store 管理本地配置，不把密钥提交到 Git。

### 云端生产环境

- Azure SQL Database 保存结构化业务数据；
- Azure Blob Storage 保存音频、原始转写和导出文件；
- Azure Container Apps 运行 Web/API 与后台 Worker；
- Azure Service Bus 负责异步处理队列；
- Azure Key Vault 与 Managed Identity 管理密钥和服务身份；
- Azure Monitor/Application Insights 记录性能、错误和工作流追踪。

### Notion 与 Markdown 的角色

Web MVP 上线后：

- Azure SQL 是唯一事实来源；
- 系统自动导出 Markdown 快照；
- Notion 作为项目规划、浏览、总结和备份镜像；
- Notion 修改必须经过显式 Import/Sync Review 才能回写数据库；
- 所有同步保存版本、来源和冲突记录。

不建立数据库、Notion 和本地 MD 的无条件三方双向同步，避免覆盖和冲突。

## 7.3 核心表

### `source_files`

记录原始材料：

- id
- title
- original_filename
- source_type
- file_path / external_url
- captured_at
- imported_at
- language
- scenario
- contains_my_speech
- audio_available
- transcript_quality
- processing_priority
- processing_status
- content_hash
- metadata_json

### `speakers`

- id
- display_name
- role（Evan / Teacher / Student / Staff / Unknown）
- native_language
- is_me
- privacy_label

### `transcripts`

- id
- source_file_id
- provider
- provider_model
- original_text
- cleaned_text
- language
- version
- created_at

### `transcript_segments`

- id
- transcript_id
- speaker_id
- segment_index
- start_ms
- end_ms
- text
- confidence
- manually_verified

### `processing_runs`

记录每次 AI 或规则处理，便于审计：

- id
- source_file_id
- task_type
- provider
- model
- prompt_version
- input_hash
- output_json
- status
- started_at
- completed_at
- error_message

### `ai_suggestions`

AI 暂存建议：

- id
- processing_run_id
- source_segment_id
- suggestion_type
- original_text
- chinese_intention
- suggested_json
- confidence
- requires_audio_check
- review_status
- reviewed_at

### `interaction_logs`

- id
- source_file_id
- event_title
- event_date
- scenario
- person_role
- what_happened
- what_they_said
- what_i_said
- what_i_intended
- what_went_wrong
- better_version
- follow_up
- reflection
- confidence

### `learning_items`

统一保存最终学习条目：

- id
- title
- item_type
- chinese_intention
- original_text
- minimum_correction
- natural_version
- easy_active_version
- explanation_cn
- trigger_text
- follow_up_text
- repair_line
- pronunciation_note
- register
- usage_mode
- learning_status
- difficulty
- priority
- last_used_at
- created_at
- updated_at

`item_type` 可包括：

- intro
- answer
- question
- story
- opinion
- repair
- closing
- my_better_version
- habit_replacement
- native_expression
- vocabulary
- collocation
- sentence_pattern
- pronunciation
- follow_up

### `learning_item_sources`

多对多关联学习条目和原始来源：

- learning_item_id
- source_file_id
- segment_id
- relation_type

### `tags` 与 `learning_item_tags`

用于 Scenario、Topic 和自定义标签。

### `review_events`

- id
- learning_item_id
- reviewed_at
- rating（Again / Hard / Good / Easy）
- response_text
- previous_status
- new_status
- next_review_at
- interval_days
- notes

### `review_schedules`

- learning_item_id
- next_review_at
- interval_days
- repetitions
- ease_factor
- suspended

### `profile_facts`

保存经用户确认的个人信息和叙事素材：

- id
- category
- fact_cn
- fact_en
- evidence_source_id
- confidence
- active
- valid_from
- valid_to

此表用于生成个人介绍，但模型不能擅自创建虚假的经历或包装。

### `sync_records`

记录 Markdown / Notion 同步：

- id
- entity_type
- entity_id
- destination
- remote_id
- local_version
- remote_version
- sync_status
- last_synced_at
- conflict_json

---

# 8. 已读、未读、顺序和每日复习设计

不要只使用一个 `is_read` 布尔值。项目中存在三种不同状态：

## 8.1 来源处理状态

```text
New → Queued → Processing → Needs Review → Processed → Archived
```

用于回答：“这个录音或文本处理了吗？”

## 8.2 AI 建议审核状态

```text
Pending → Accepted / Edited / Rejected / Merged
```

用于回答：“模型生成的建议确认了吗？”

## 8.3 学习状态

```text
Unread → Ready → Learning → Active → Mastered → Archived
```

用于回答：“这条表达我读过、练过或掌握了吗？”

## 8.4 顺序处理

每个来源保存：

- `captured_at`；
- `processing_priority`；
- `queue_position`；
- `segment_index`。

Inbox 默认按以下顺序：

1. 用户手动置顶；
2. 近期即将使用的场景；
3. 含 Evan 大量讲话的真实对话；
4. 尚未处理的旧材料；
5. 课堂和参考材料。

## 8.5 每日复习

第一版使用简单间隔：

```text
第一次确认：当天
第一次复习：1 天后
第二次复习：3 天后
第三次复习：7 天后
第四次复习：14 天后
第五次复习：30 天后
```

后期根据 Again / Hard / Good / Easy 改为 SM-2 或类似算法。

每日页面包括：

- Due Today；
- New / Unread；
- My Common Mistakes；
- Upcoming Scenario；
- Recently Used；
- Quick Add。

---

# 9. Web 平台功能规划

## 9.1 Dashboard

显示：

- 今日到期复习；
- 未处理来源；
- 待审核 AI 建议；
- 最近真实交流问题；
- 本周反复错误；
- 本周新增本地表达；
- 即将到来的场景包；
- 学习统计。

## 9.2 Inbox / Source Index

功能：

- 上传和批量导入；
- 文件去重；
- 显示来源、日期和处理状态；
- 重命名 `Translate 1/2/...`；
- 标记说话人和场景；
- 设置处理优先级；
- 顺序浏览；
- 播放音频并跳转时间点。

## 9.3 Transcript Viewer

功能：

- 左侧音频，右侧带时间戳文本；
- 按说话人显示；
- 手动纠正文本；
- 选中片段后运行指定分析；
- 标记“这是我说的”；
- 添加 Chinese Intention；
- 生成 Better Version；
- 将片段加入复习。

## 9.4 AI Review Queue

功能：

- 并排显示原文、上下文和建议；
- 显示问题类型和置信度；
- 接受、编辑、拒绝、合并；
- 回听音频；
- 选择 Active / Understand Only；
- 一键加入 Interaction Log 或 Learning Item。

## 9.5 Prepared Expression Bank

视图：

- Conversation Survival；
- Daily Life；
- University；
- Networking；
- Current Projects；
- Personal Narrative；
- My Common Mistakes；
- Native Expressions；
- Vocabulary & Pronunciation；
- Interview Language；
- Ready to Practise；
- Mastered。

## 9.6 Review Mode

复习形式：

- 看中文说英文；
- 看 Trigger 回答；
- 填空；
- 对比 Original 和 Better Version；
- 听音频识别表达；
- 录音复述；
- Role-play 问答；
- 自评 Again / Hard / Good / Easy。

## 9.7 Quick Add

支持手动加入：

- 今天不会说的一句话；
- 今天没听懂的一句话；
- 想提前准备的一段中文；
- 新词汇或搭配；
- 一个即将发生的场景。

## 9.8 Search

第一阶段：

- Azure SQL Full-Text Search；
- 按 Scenario、Topic、Speaker、Source、Status 和 Date 筛选；
- 使用标准索引、复合索引和查询计划优化常用页面；
- 对 JSON 字段使用 SQL Server JSON functions，而不是把所有数据塞进无结构文本。

后期：

- Azure AI Search 或 Azure SQL 向量能力；
- 相似表达检索；
- 自动去重；
- “我以前是否表达过类似意思？”；
- 根据新场景召回相关个人经历和已审核表达。

语义搜索属于后期能力。第一版先用清晰的数据模型、Full-Text Search 和字段筛选完成主要需求。

# 10. 企业技术栈研究与最终云平台决策

## 10.1 研究问题

本项目的技术选型不再以“哪个最方便”作为第一标准，而以以下目标排序：

1. 对塔斯马尼亚本地就业是否有迁移价值；
2. 是否能展示企业级云应用开发能力；
3. 是否能连接数据库、Web、AI、DevOps、安全和可观测性；
4. 是否适合由一个人先完成模块化 MVP，再逐步扩展；
5. 是否能与 CLAW 实习中的 AI Workflow、Context Processing 和 Node.js 经验形成连续叙事。

研究证据来自公开政府合同、企业岗位和官方技术文档。公开招聘只能代表可观察样本，不等于塔州所有企业的精确市场份额。

## 10.2 云平台结论

### Primary Cloud：Microsoft Azure

本项目最终以 **Microsoft Azure** 作为主云平台。

原因：

- 塔斯马尼亚政府具有持续的 Microsoft 采购与授权体系；
- 塔州港口、能源、健康、咨询和企业应用岗位公开出现 Azure、Microsoft 365、Entra ID、Power Platform、Dynamics 365、.NET、Azure DevOps、Power BI 和 SQL Server；
- Azure 能让本项目同时覆盖应用部署、关系数据库、对象存储、身份、安全、异步任务、监控和 AI；
- 对本地政府、能源、公共服务和中型企业岗位的迁移价值更直接。

### Secondary Cloud Knowledge：AWS

AWS 不作为本项目主部署平台，但应保留基础认知：

- IAM、VPC、S3、RDS、ECS/Lambda 与 CloudWatch 的对应概念；
- Terraform 的跨云用法；
- Azure 服务与 AWS 服务的架构映射。

原因是塔州也存在面向全国性企业的 AWS/GCP Cloud Engineer 岗位，例如金融机构的大规模 AWS/GCP 环境。AWS 能力对全国远程岗位仍然有价值。

### GCP：Awareness Only

GCP 暂不进入本项目主实现。当前公开样本中，GCP 更多作为大型组织的第二云或特定岗位要求，塔州本地通用信号弱于 Azure。

## 10.3 最终云服务映射

| 系统能力               | 最终选择                                             | 说明                                                                  |
| ---------------------- | ---------------------------------------------------- | --------------------------------------------------------------------- |
| Web Frontend           | Next.js + TypeScript                                 | 现代企业 Web、SSR/Server Components、与 Node 生态一致。               |
| Backend API            | NestJS + TypeScript                                  | 模块化、Dependency Injection、Validation、Testing，适合企业后端结构。 |
| Database               | Azure SQL Database                                   | 对齐 SQL Server/T-SQL 与本地 Microsoft 数据生态。                     |
| ORM                    | Prisma                                               | TypeScript 类型安全；支持 SQL Server connector。                      |
| Object Storage         | Azure Blob Storage                                   | 保存音频、转写、导出和附件；默认私有容器。                            |
| Application Hosting    | Azure Container Apps                                 | 部署 API、Web 和后台处理容器；支持 Jobs 与事件驱动任务。              |
| Queue                  | Azure Service Bus                                    | 解耦上传、转写、AI 分析、同步和复习生成。                             |
| Secrets                | Azure Key Vault                                      | 保存 AI API Key、连接密钥和外部服务凭据。                             |
| Identity               | Managed Identity + Microsoft Entra ID                | 服务间免密访问与后续用户认证。                                        |
| Observability          | Azure Monitor + Application Insights + OpenTelemetry | 日志、Trace、指标、错误和 AI Workflow 性能。                          |
| CI/CD                  | GitHub Actions → Azure                               | 与 Git/Codex 工作流直接连接；同时理解 Azure DevOps 概念。             |
| Infrastructure as Code | Terraform Primary；Bicep Awareness                   | Terraform 兼顾 Azure 与跨云岗位；Bicep 用于理解 Azure 原生 IaC。      |
| AI Primary             | Azure OpenAI                                         | 结构化输出、企业身份与 Azure 集成。                                   |
| AI Secondary           | Anthropic Claude Adapter                             | 用于模型对比、Fallback 或特定长上下文任务。                           |
| Speech-to-Text         | Azure AI Speech Provider + Transcript Import         | 云端转写；同时允许导入 Notta、Otter、Zoom 等现有 TXT。                |

## 10.4 参考证据与官方资料

### Tasmania / Local Market Evidence

- Tasmanian Government Microsoft Agreement: https://www.purchasing.tas.gov.au/contracts/common-use-contracts-of-the-tasmanian-government/common-use-contracts-index/microsoft-agreement
- Tasmanian Government Microsoft Licensing Solutions Provider: https://www.purchasing.tas.gov.au/contracts/common-use-contracts-of-the-tasmanian-government/common-use-contracts-index/tglsp
- Hydro Tasmania Senior Software Developer: https://au.linkedin.com/jobs/view/senior-software-developer-at-hydro-tasmania-4439136108
- Tassal Business Applications Developer / Software Engineer: https://au.linkedin.com/jobs/view/business-applications-developer-software-engineer-at-tassal-group-4379781801
- Insignia Financial Cloud Engineer (AWS/GCP evidence): https://au.linkedin.com/jobs/view/cloud-engineer-at-insignia-financial-4436168189
- Tasmania Azure Cloud Engineer example: https://au.linkedin.com/jobs/view/cloud-engineer-at-2-doors%C2%AE-4441164852

### Official Technical References

- Azure SQL Database: https://learn.microsoft.com/azure/azure-sql/database/sql-database-paas-overview
- Azure SQL Full-Text Search: https://learn.microsoft.com/sql/relational-databases/search/full-text-search
- Azure Blob Storage with JavaScript: https://learn.microsoft.com/azure/storage/blobs/storage-quickstart-blobs-nodejs
- Azure Container Apps: https://learn.microsoft.com/azure/container-apps/overview
- Azure Container Apps Jobs: https://learn.microsoft.com/azure/container-apps/jobs
- Azure OpenAI Structured Outputs: https://learn.microsoft.com/azure/ai-foundry/openai/how-to/structured-outputs
- Azure Key Vault: https://learn.microsoft.com/azure/key-vault/general/overview
- Azure Monitor / Application Insights: https://learn.microsoft.com/azure/azure-monitor/app/app-insights-overview
- Prisma SQL Server connector: https://www.prisma.io/docs/orm/overview/databases/sql-server

---

# 11. AI、Codex 与 CLAW 式工作流架构

## 11.1 Codex 的角色

Codex 负责开发这个系统，而不是作为每次运行都要人工打开的翻译网页：

- 创建和维护 Monorepo；
- 编写 Next.js、NestJS、Prisma 和 Azure 集成代码；
- 创建数据库 Schema 与 Migration；
- 编写 Prompt、Few-shot 示例、JSON Schema 与 Zod 验证；
- 编写测试、Evaluation 和数据导入脚本；
- 部署基础设施与 CI/CD；
- 修复 Bug、重构并维护技术文档。

## 11.2 系统运行时 AI Provider

系统不使用本地模型作为正式方案。运行时 Provider 设计为：

```text
AIProvider
├── AzureOpenAIProvider   # Primary
├── AnthropicProvider     # Secondary / Fallback / Comparison
└── MockAIProvider        # Local development and tests only
```

业务层只调用稳定接口：

- `analyseTranscript()`
- `analyseMySpokenOutput()`
- `extractNativeExpressions()`
- `convertChineseNarrative()`
- `generateLearningItems()`
- `generateReviewQuestions()`

Provider 负责把统一请求转换为不同模型 API，并返回统一的结构化结果。

## 11.3 Phase 2：复现并扩展 CLAW 中的 AI Workflow 思路

Phase 2 要有意识地复现用户在 CLAW 实习中接触的技术流程，但应用到个人英语领域：

```text
1. Source Ingestion
   上传音频、TXT、DOCX 或粘贴文本

2. Source Classification
   判断语言、来源类型、场景、是否包含 Evan 发言

3. Segmentation and Speaker Mapping
   切分片段，识别/人工确认 Evan、Teacher、Staff、Student 等角色

4. Context Processing
   读取当前片段上下文、个人背景、历史已审核表达、常见错误、近期场景

5. Workflow Routing
   选择“分析我的表达”“提取本地表达”“中文叙事转英文”等工作流

6. Prompt Assembly
   组合 System Prompt、任务 Prompt、Few-shot 示例、上下文窗口和输出 Schema

7. Model Interaction
   Azure OpenAI 为主；Claude Adapter 用于对比、Fallback 或特定任务

8. Multi-turn Workflow
   根据验证结果或用户反馈继续修正、补充或重新生成，而不是一次调用结束

9. Structured Output Validation
   使用 JSON Schema + Zod 做字段、枚举、长度和必填项验证

10. Semantic and Rule Validation
    检查是否改变个人事实、是否超出上下文、是否把转写错误误判为语法错误

11. AI Suggestions Staging
    所有结果先进入待审核区，不直接成为正式学习材料

12. Human-in-the-loop Review
    用户 Accept、Edit、Reject、Merge 或 Regenerate

13. Transactional Commit
    通过数据库事务写入 Learning Item、来源关系和复习计划

14. Evaluation and Observability
    保存模型、Prompt 版本、Token、延迟、错误、用户修改和最终接受结果
```

## 11.4 与 CLAW 实习经历的技术映射

| CLAW 相关经验                               | Personal English Bank 对应实现                                      |
| ------------------------------------------- | ------------------------------------------------------------------- |
| OpenAI Codex / Anthropic Claude integration | Azure OpenAI / Anthropic Provider Adapter                           |
| System prompts                              | 每类英语分析任务的 System Prompt                                    |
| Few-shot prompting                          | 使用已审核的真实案例作为 Few-shot Dataset                           |
| Context management                          | 个人资料、历史错误、场景、上下文和已掌握表达的召回                  |
| Model interaction logic                     | Provider、重试、Fallback、温度、Token 与错误处理                    |
| Multi-turn interactions                     | AI 建议 → 用户修改 → 再生成 → 验证 → 入库                           |
| Analysis and context-processing features    | Transcript 分类、Speaker、Intent、错误与自然表达分析                |
| Web workflow features                       | Inbox、Transcript Viewer、Review Queue、Learning Bank、Daily Review |
| Node.js backend integrations                | NestJS、Service Bus、Azure SDK、Prisma 和模型 SDK                   |
| Workflow validation                         | JSON Schema、Zod、规则验证、测试和 Evaluation                       |

这使项目不仅是英语学习网站，也成为一个可展示的 **domain-specific AI workflow application**。

## 11.5 Prompt 与 Few-shot 管理

Prompt 必须版本化：

- `prompt_key`
- `prompt_version`
- `system_prompt`
- `task_prompt`
- `schema_version`
- `few_shot_set_id`
- `model_policy`
- `created_at`
- `active`

Few-shot 示例来自已审核内容，不能直接使用未经确认的模型输出。

## 11.6 Structured Output 与数据库写入

Azure OpenAI Structured Outputs 用于强制模型返回符合 JSON Schema 的结构。

即便 Schema 验证通过，仍然不能让模型直接写正式数据库：

```text
Model Output
→ JSON Schema / Zod Validation
→ Rule Validation
→ AI Suggestions
→ Human Review
→ Application Service
→ Database Transaction
```

这条规则在所有阶段保持不变。

## 11.7 中文个人录音的云端流程

```text
中文录音
→ Azure AI Speech 转写中文
→ Blob Storage 保存原始音频与转写
→ Context Processor 提取事实、观点和性格信息
→ Azure OpenAI 生成 Easy Active English / Natural English
→ 生成 10/30/60 秒版本
→ Zod 验证
→ Review Queue
→ 用户确认
→ Azure SQL + Review Schedule
```

保留中文原文和音频，以便检查英文是否改变原意。系统不依赖 ChatGPT 网页版，也不使用本地 LLM。

---

# 12. 推荐技术架构

## 12.1 架构原则

第一版采用 **Cloud-first Modular Monolith**：

- 生产系统部署在 Azure；
- 本机只作为开发、测试和调试环境；
- 不在第一版拆成大量微服务；
- Web/API 与 Worker 可以分别打包成容器，但共享 Domain、Schema 和数据库模型；
- 异步任务通过 Service Bus 解耦；
- AI、Speech、Storage 和 Database 均通过 Adapter/Repository 封装，避免业务逻辑依赖具体 SDK。

## 12.2 最终技术栈

- Language：TypeScript；
- Frontend：Next.js；
- Backend：NestJS；
- Database：Azure SQL Database / SQL Server；
- ORM：Prisma；
- Validation：Zod + JSON Schema；
- Background Processing：Azure Service Bus + Container Apps Worker/Jobs；
- Storage：Azure Blob Storage；
- Identity：Managed Identity，后续加入 Microsoft Entra ID；
- Secrets：Azure Key Vault；
- Monitoring：Azure Monitor、Application Insights、OpenTelemetry；
- AI：Azure OpenAI Primary + Anthropic Claude Adapter；
- Transcription：Azure AI Speech Provider + Existing Transcript Import；
- Testing：Vitest/Jest、Supertest、Playwright；
- CI/CD：GitHub Actions；
- IaC：Terraform；
- Local Development：Docker Compose + SQL Server Developer Edition + Mock providers。

选择 TypeScript/Node.js 的原因：

- 与 CLAW 实习中的 JavaScript/Node.js 后端和 Web Workflow 经验连续；
- 前后端、Schema、Validation 和模型类型可以共享；
- NestJS 的模块、Dependency Injection、Guard、Interceptor 和 Testing 更适合展示企业后端能力；
- Azure SDK、OpenAI/Anthropic SDK、Prisma 和现代 Web 生态完整；
- Python 后续可作为独立的数据处理服务，但不再是主应用语言。

## 12.3 推荐仓库结构

```text
personal-english-bank/
├── AGENTS.md
├── README.md
├── package.json
├── pnpm-workspace.yaml
├── turbo.json
├── docker-compose.yml
├── .env.example
├── apps/
│   ├── web/                    # Next.js
│   ├── api/                    # NestJS API
│   └── worker/                 # Service Bus / background jobs
├── packages/
│   ├── domain/                 # entities, rules, status transitions
│   ├── database/               # Prisma client and repositories
│   ├── schemas/                # Zod and JSON Schema
│   ├── ai/                     # provider interface and orchestration
│   ├── prompts/                # versioned prompts and few-shot sets
│   ├── azure/                  # Blob, Service Bus, Key Vault adapters
│   ├── config/
│   └── observability/
├── prisma/
│   ├── schema.prisma
│   ├── migrations/
│   └── seed.ts
├── infra/
│   └── terraform/
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── scripts/
│   ├── import-existing-texts.ts
│   ├── export-markdown.ts
│   └── sync-notion.ts
└── docs/
    ├── product-plan.md
    ├── architecture.md
    ├── database-design.md
    ├── ai-workflows.md
    └── processing-rules.md
```

## 12.4 本地开发与云端生产的边界

### Local Development

```text
Developer PC
├── Next.js dev server
├── NestJS API / Worker
├── SQL Server Developer container
├── Optional Azurite for Blob-compatible testing
├── Mock AI/Speech Providers
└── Azure dev subscription for integration tests
```

### Azure Production

```text
Users
  ↓
Azure Container Apps / Web + API
  ↓
Azure SQL Database
Azure Blob Storage
Azure Service Bus
Azure OpenAI / Azure AI Speech
Azure Key Vault / Managed Identity
Azure Monitor / Application Insights
```

本地开发环境用于降低开发成本和快速调试，但生产架构从第一天就按 Azure 服务边界设计。

## 12.5 Codex 项目规则

仓库 `AGENTS.md` 必须明确：

- 本规划是产品、数据和技术选型的最高依据；
- Primary Cloud 固定为 Azure，主数据库固定为 Azure SQL；
- 不得自行改回 SQLite、PostgreSQL、本地 LLM 或纯本地部署；
- 不得直接删除原始音频和转写；
- 不得让 LLM 直接执行数据库写入；
- 所有 AI 输出必须经过 JSON Schema/Zod 验证；
- 最终语料必须可追溯到原始来源；
- 对发音的结论必须有音频依据；
- 不得擅自新增或包装个人经历；
- 默认英文必须简单、自然、容易说；
- 所有数据库变更必须使用 migration；
- 数据库写入必须使用事务；
- Prompt、Few-shot、Schema 和模型策略必须版本化；
- 每个核心功能必须包含单元、集成或端到端测试；
- 云资源必须由 Terraform 管理，禁止只在 Portal 手工创建而不记录；
- Secret 不得提交 Git，生产访问优先使用 Managed Identity。

---

# 13. 云安全、隐私、质量与可追溯性

录音可能包含老师、同学、工作人员和项目伙伴的信息。云端方案必须遵守：

## 13.1 数据安全

- Azure Blob 容器默认 Private；
- Azure SQL、Blob、Service Bus 和 AI 服务使用 RBAC 与最小权限；
- 服务间优先使用 Managed Identity；
- Secret 与第三方 API Key 存入 Azure Key Vault；
- 传输与静态数据加密；
- 区分 Development、Test 和 Production 环境；
- 生产数据库不使用共享管理员账号作为应用连接；
- 根据成本和阶段逐步加入 Private Endpoint、网络限制和 WAF。

## 13.2 隐私与来源控制

- 上传或调用云 AI 前记录数据来源与处理授权；
- 对人物默认使用角色标签，不必保存真实姓名；
- 支持针对来源禁用 AI 处理；
- 支持删除来源及其派生数据；
- 支持导出个人数据；
- 原始音频、原始转写、清理文本和 AI 版本分别保存；
- 对敏感录音设置更短保留期或只保存经选择的片段。

## 13.3 AI 质量控制

- 自动结果只进入 Staging；
- 显示模型、Prompt、Few-shot Set、Schema 和时间；
- 显示置信度与不确定性；
- 不能仅凭转写判断发音；
- 检查是否改变个人事实；
- 检查是否把转写错误误判为语言错误；
- 保存用户 Accept/Edit/Reject 数据，用于后续 Evaluation；
- 模型升级前使用固定测试集回归验证。

## 13.4 可观测性

每次 Workflow 至少记录：

- `correlation_id`
- `source_file_id`
- `processing_run_id`
- `workflow_name`
- `provider`
- `model`
- `prompt_version`
- `schema_version`
- `latency_ms`
- `token_usage`
- `status`
- `retry_count`
- `error_code`

日志不得直接输出完整敏感录音内容或 API Key。

---

# 14. MVP 与云开发路线

## Phase 0｜项目初始化与可执行基线

- 固化本规划与 `AGENTS.md`；
- 创建 TypeScript Monorepo；
- 建立 Next.js、NestJS、Prisma 和 SQL Server 本地开发环境；
- 建立 Lint、Test 和 Migration 基线；
- 准备 10–20 个代表性语料测试片段。

Azure Dev/Test Subscription、Terraform、部署流水线、资源命名和 Tag 规则移至 Phase 1.5，避免阻塞产品闭环验证。

## Phase 1｜无 AI 的本地学习闭环 MVP

必须完成：

- 本地 SQL Server Schema 与 Migration；
- 从配置的 `TRANSLATION_ROOT` 发现由 Codex 生成的 `*.curated.json`；
- 支持音频/视频和现成文本两种系统外输入；音频/视频转写由 Codex 调用本地工具完成；
- 为每个来源保存保真双语文本、精修双语文本、来源日期和中文摘要；
- 只把已筛选的精修双语句子写入 Segment、Learning Item 和 Review Schedule；
- Source Index；
- 精修 Transcript 与逐句双语浏览；
- Source 与 Learning Item 的 Create / Read / Update / Delete；
- 按文件、日期、摘要、英文和中文查询；
- 稳定的 Curated Import API，供 Codex 和未来 AI Provider 复用；
- 状态、标签、已读/未读和顺序处理；
- 1/3/7/14/30 天简单复习；
- “口述一次并点击完成”的复习动作；可选保存外部语音工具产生的文字，但不做正确性判断；
- 每日复习上限；
- 真实使用记录；
- SQL Server Full-Text Search 或第一版字段搜索；
- Markdown 导出；

这一阶段应用内部不接正式 AI，也不要求 Azure 部署。Codex 暂时承担 Curator 的角色，负责转写、翻译、保真整理、精修、筛选、摘要和结构化打包；Web/API 只验证数据库、查询、CRUD、学习和复习流程。第一批只处理 5 个代表性来源；Schema 和 Curated Import Contract 稳定后再批量处理其余材料。

## Phase 1.5｜Azure 部署与企业作品集扩展

- Azure SQL Database；
- Azure Blob Storage；
- Container Apps；
- Key Vault 与 Managed Identity；
- Application Insights；
- Terraform；
- GitHub Actions CI/CD；
- Development / Test / Production 环境边界；
- 月度预算与成本告警。

## Phase 2｜CLAW 式 AI Workflow

- 首个工作流只实现 `analyse_my_spoken_output`；
- 第一版只启用 Azure OpenAI Provider；
- System Prompt 与 Prompt Version；
- Few-shot Dataset；
- Context Processor；
- Workflow Router；
- Multi-turn Interaction Logic；
- JSON Schema + Zod；
- Processing Run 与 AI Suggestions；
- Human Review Queue；
- Accept、Edit、Reject、Merge、Regenerate；
- Retry、Fallback、Timeout 和错误记录；
- Application Insights Trace 与 AI Evaluation；
- 将 CLAW 中的 Prompt、Context、Model Interaction 和 Node.js Workflow 经验完整迁移到该项目。

当首个工作流的接受率、编辑率、事实保持率和单位成本达到验收标准后，再增加 Anthropic Claude Adapter、中文叙事和本地表达提取。

## Phase 3｜复习系统增强

- 基于反馈调整间隔；
- My Common Mistakes；
- Upcoming Scenario；
- 学习统计；
- 听力、录音复述与 Role-play；
- 更丰富的真实使用分析。

## Phase 4｜云端音频与转写

- Azure AI Speech；
- 音频播放与时间戳；
- Worker/Container Apps Jobs；
- Service Bus 队列；
- Speaker 人工校正；
- 发音待检查；
- Notta/Otter/Zoom 现有转写导入。

## Phase 5｜中文个人叙事与个性化 Context

- 中文录音；
- 中文原始转写；
- Easy Active English；
- 10/30/60 秒版本；
- Profile Facts；
- Personal Narrative；
- 防止虚构和事实冲突；
- 从已审核数据中召回 Few-shot 和相关上下文。

## Phase 6｜语义搜索与高级分析

- Azure AI Search 或 Azure SQL Vector；
- 相似表达；
- 自动去重；
- 个人错误趋势；
- 本地表达趋势；
- 个性化场景包；
- Prompt/Model A/B Evaluation。

## Phase 7｜Notion 与 Markdown 同步

- Azure SQL → Markdown；
- Azure SQL → Notion；
- 显式 Import Review；
- 冲突检测；
- 同步日志；
- 本地目录 `E:\\MaskPlan\\Personal English Bank` 与 Notion 页面保持规划和导出同步。

## 第一条端到端 User Story

> 用户从配置的 `TRANSLATION_ROOT` 导入一份 AR Glass 文本；系统在本地 SQL Server 创建 Source、Asset、Transcript 和 Segment；用户确认 Speaker，选择 Evan 的一个片段，手动填写 Chinese Intention 和 Easy Active Version，保存为 Learning Item，并在下一次到期时完成复习和记录真实使用。

这条 User Story 跑通后，项目核心闭环成立：

```text
Source
→ Transcript / Segment
→ Human Selection
→ Learning Item
→ Daily Review
→ Real-world Usage
```

Phase 2 再把其中的“Human Selection → Learning Item 草稿”升级为：

```text
Context Processing
→ AI Workflow
→ Human Review
→ Transactional Commit
```

# 15. 内容建设优先级

长期优先顺序：

```text
真实日常交流
>
自己的输出、错误和个人表达
>
澳洲老师与本地人的自然表达
>
高频词组、句型和发音
>
课堂与项目专业表达
>
Networking 与职业口语
>
通用面试专题
```

第一批内容：

## Conversation Survival

- 请求重复；
- 请求慢一点；
- 确认理解；
- 确认数字、日期和地址；
- 请求拼写；
- 不知道词时换一种说法；
- 请求邮件发送；
- 争取思考时间；
- 修正自己的回答；
- 自然结束对话。

## Personal Narrative

- 10/30/60 秒自我介绍；
- 为什么来 UTAS；
- 教育和实习背景；
- AI Factory；
- Motorsport LiDAR；
- AR/VR 创业；
- 职业兴趣；
- 想寻找的项目和机会。

## Daily Life and University

- 志愿机会询问；
- 向 ICT 工作人员咨询；
- 课堂求助；
- 小组任务沟通；
- 食物、宿舍、银行、交通和服务；
- 日常 Small Talk。

## My Common Patterns

- 高频填充词；
- 重复的语法问题；
- 容易混淆的词；
- 过长或没有重点的回答；
- 转写中反复识别错误的发音词。

---

# 16. 日常和每周运行方式

## 每次新增录音

1. 将 MP3/MP4 或已准备文本放入本地 `TRANSLATION_ROOT`；
2. 系统外 Curator 使用本地工具完成转写（仅音视频需要）；
3. 保留完整原始文本，并生成逐句双语原始版本；
4. 生成纠错、去除无效内容后的逐句双语精修版本；
5. Curator 完成筛选、摘要和来源日期整理，输出版本化 Curated Import Package；
6. 应用只通过 Curated Import Contract 原子导入已完成包；
7. 数据库保存完整原文、完整原始双语、完整精修双语，并只把筛选句加入复习；
8. 用户在来源页可查看全文、搜索、修改或删除来源与学习句；
9. 每日按 1/3/7/14/30 天节奏完成有上限的口语复习；
10. 未完成文件只进入待处理清单，不导入数据库，也不由应用运行时调用 AI。

## 每天 5–15 分钟

- 处理一个短对话，或长录音中的 5–10 分钟；
- 完成今日到期复习；
- 快速记录一条没听懂、不会说或想提前准备的内容；
- 不追求一次处理整个课堂。

## 每周 30–45 分钟

- 查看最常见的三个表达问题；
- 查看重复最多的填充词；
- 选择五个本地自然表达；
- 选择五个主动词组；
- 检查哪些表达已真实使用；
- 缩短太长或太正式的内容；
- 准备下周最可能出现的场景；
- 导出一份 Weekly Review。

---

# 17. 当前最先执行的任务

1. 已完成 `AGENTS.md`、TypeScript Monorepo、本地 SQL Server、Prisma Schema 和受控 Migration；
2. 已完成版本化 Curated Import Contract、幂等批量导入、Source/Learning Item CRUD、搜索和 Markdown 导出；
3. 已完成完整原文、完整原始双语和完整精修双语的数据库保存与全文预览，不采用行数截断；
4. 已完成 1/3/7/14/30 天复习、每日上限和不评分的口语完成流程；
5. 当前继续用真实语料验证本地 Product Core，并修复使用中发现的问题；
6. 尚未精修的来源由用户交给后续 GPT/Curator，完成后继续使用同一导入合同；
7. 核心闭环稳定后再进入 Phase 1.5 Azure 部署与 Phase 2 运行时 AI Provider。

当前已验证的可运行用户故事：

> Curator 在应用外处理一份 AR Glass 或课堂转写，保留完整原文和两份完整双语版本，筛选可学习句并通过合同导入；用户能够按文件和日期定位来源、查看全部内容、搜索或删除来源、编辑句子，并从次日开始完成有上限的口语复习。

这条用户故事完成后，项目的核心闭环就已经成立。

---

# 18. 最终原则

1. 原始材料永远保留，AI 结果永远可追溯；
2. 数据先进入 Inbox 和 Staging，不直接成为主动语料；
3. 先解决真实交流，再扩展专题；
4. 先准备简单、自然、能说出的英文，再追求高级表达；
5. 不仅纠错，还要判断原意、自然度和对话效果；
6. 不把自动转写错误误判成口语错误；
7. 发音结论必须有音频依据；
8. 澳洲本地表达需要区分主动使用与仅需听懂；
9. 面试方法论归 MyLinkedIn，英语表达归 Personal English Bank；
10. Azure SQL Database 是未来唯一主数据源，Notion 和 Markdown 是规划、视图、镜像和导出；
11. Codex 负责开发系统，运行时 AI 通过 Azure OpenAI 或 Anthropic Provider API 完成；
12. 模型输出必须结构化、验证、审核和版本化；
13. 不追求条目数量，追求真实使用率；
14. 系统最终服务于一个目标：让 Evan 在澳洲学习、生活、项目和职业交流中更自然、更稳定地表达自己。

---

# 19. 修改记录

## 2026-07-27

- 将当前工作边界调整为“Codex 负责系统开发；剩余语料交由后续 GPT/Curator 处理”；
- 明确只导入已经完整生成并通过校验的 Curated Import Package，未完成文件输出独立清单；
- 将批量导入从固定 5 个扩展为应用单批最多 20 个、脚本自动分批导入全部等待包；
- 为 Transcript 增加 `sourceText`，分别保存完整原文件、完整原始双语和完整精修双语；
- 来源详情页取消节选、行数限制和分页，显示三份完整文本及字符数；
- 2026-07-27 本地批次校验结果：46 个原文件中 24 个已完成、22 个未完成；24 个已完成来源全文逐字符一致，共 467,310 个原文字符和 700 条筛选复习句。
